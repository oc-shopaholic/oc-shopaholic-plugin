# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    active:
        label: 'lovata.toolbox::lang.field.active'
        type: switch
        conditions:
            - active <> true
            - active = true